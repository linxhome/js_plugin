(function($) {
    $.fn.plugin = function(){
        var default = {key,"value"}
        var options = $.extend(default,options); 
        this.each(function() {
            //实现代码
        });



    };
})(jQuery);
